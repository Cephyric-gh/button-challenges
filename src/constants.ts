export const SCROLL_TOP_PAD = 'max(6.5rem, calc(env(safe-area-inset-top) + 4.5rem))';

/** Hard limit for how many rows we render (performance). */
export const MAX_LIST_ROWS = 5000;

export const CURRENT_STEP_STORAGE_KEY = 'button-challenges-current-step';

export const DEFAULT_STEP_WINDOW = 10;

export const SECONDARY_BTN_CLASS =
  'rounded-xl border border-border bg-canvas/60 px-3 py-2 text-xs font-semibold text-ink shadow-inner shadow-black/15 outline-none transition hover:border-accent/35 hover:bg-canvas/80 disabled:cursor-not-allowed disabled:opacity-40 sm:text-sm';
